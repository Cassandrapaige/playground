import React from "react";

// components
import Layout from "../components/layout/layout.component";
import PhoneBody from "../components/phone-body/phone-body.component";

const Stopwatch = () => {
  return (
    <Layout bgColor='#af91db'>
      <PhoneBody></PhoneBody>
    </Layout>
  );
};

export default Stopwatch;
